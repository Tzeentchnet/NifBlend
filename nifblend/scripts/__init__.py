"""Bundled non-Python scripts shipped with the NifBlend extension.

Currently hosts the xEdit ``.pas`` companion script used by Phase 8e's
cell-layout import workflow. Loaded via :func:`importlib.resources.files`.
"""
